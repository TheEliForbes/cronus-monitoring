stream
	|from()
		.database('telegraf')
		.retentionPolicy('autogen')
		.measurement('cpu')
	|alert()
		.warn(lambda: int("usage_idle") < 100)
	.message('cpu test')
	.slack()
	.iconEmoji(':exclamation:')
	