suite: test deployment
templates:
  - deployment.yaml
tests:
  - it: should work
    set:
      image.tag: 1.6.3-alpine
    asserts:
      - isKind:
          of: Deployment
      - matchRegex:
          path: metadata.name
          pattern: -influxdb$
